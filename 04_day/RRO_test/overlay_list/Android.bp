java_binary {
    name: "list_overlays",
    srcs: ["ListOverlays.java"],

    // Hidden API 접근을 위해 프레임워크 라이브러리 참조
    libs: [
        "framework",
        "services",
    ],

    // 메인 클래스 지정
    main_class: "ListOverlays",

    // 안드로이드 16(API 36 예상) 환경 대응
    sdk_version: "core_platform",
}
